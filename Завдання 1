# Користувач вводить довжину масиву
n = int(input("Введіть кількість елементів масиву: "))

# Введення елементів масиву через пробіл
array = list(map(int, input("Введіть елементи масиву через пробіл: ").split()))

# Перевіряємо, що користувач ввів саме n елементів
if len(array) != n:
    print("Помилка: кількість введених елементів не відповідає заданій довжині.")
else:
    product = 1
    found = False  # прапорець, чи є такі елементи

    for num in array:
        if num != 0 and num % 2 != 0:  # непарний і не нуль
            product *= num
            found = True

    if found:
        print("Добуток ненульових непарних елементів:", product)
    else:
        print("У масиві немає ненульових непарних елементів.")
